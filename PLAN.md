# NtoNTier 项目规划书 v1.1

> 规划日期：2026-09-03　·　状态：已自查修订，可进入实现
> 一句话目标：做一个**小白也能秒懂的 Windows 虚拟局域网联机工具**，复刻视频里 TDgame 的联机功能，
> 底层用开源 n2n 引擎（GPL-3.0），无服务器场景用 playit.gg 免费中继。

---

## 0.1 自查修订记录（v1.0 → v1.1，已并入正文）

| # | 问题 | 修订 |
|---|---|---|
| 1 | edge 创建虚拟网卡需管理员权限 | 客户端 manifest `requireAdministrator`，启动一次 UAC |
| 2 | 网络测试阻塞 UI 线程 | 测试放后台线程，结果回调刷新 |
| 3 | 手写 JSON 解析有隐患 | 改用 DataContractJsonSerializer |
| 4 | 进程按名匹配 edge 误判 | 按完整路径过滤进程 |
| 5 | "自动获取IP"未联动禁用输入 | 补真实联动逻辑 |
| 6 | 防火墙只放服务器端口 | 同时放行服务器端口 + edge 本机端口 |
| 7 | 暗色下 ComboBox 丑 | 自绘 OwnerDraw |
| 8 | 无日志入口 | "更多"菜单加"查看日志" |
| 9 | 双 edge 同机测试需不同网卡/端口 | 写入测试方案，联机配置已支持 |

## 0. 规划要点一览（给用户速览）

| 项 | 决定 |
|---|---|
| 程序名 | **NtoNTier** |
| 技术栈 | C# WinForms（.NET Framework 4.8，系统自带编译器，单 exe、免安装） |
| 主题 | 亮色 + 暗色双主题，一键切换，记忆上次选择 |
| 引擎 | n2n 3.1.1 r1255（edge.exe 客户端 / supernode.exe 服务端，与用户原 supernode 同版本） |
| 中继 | playit.gg（免费 UDP/TCP，无需服务器）；Cloudflare 免费版不支持 UDP 已排除 |
| 交付 | ①客户端单 exe ②服务端管理包 ③小白部署说明 ④本地联调验证 |
| 许可 | 遵循 n2n GPL-3.0，文档中标注来源 |

---

## 1. 需求拆解（验收清单）

### 1.1 客户端 NtoNTier.exe（主交付物）
界面（暗/亮可切换）：
- [ ] 主窗口：左侧导航（联机功能 / 在线成员 / 网络测试 / 使用指南）+ 右上主题切换
- [ ] 联机功能页：服务器选择（官方/自定义两 tab）+ 虚拟 IP 配置 + 连接/断开按钮 + 状态指示
- [ ] 在线成员页：连接后列出虚拟局域网成员（自动刷新）
- [ ] 网络测试页：Ping / TCP 端口 / UDP 探测，带结果输出框
- [ ] 使用指南页：内置小白图文说明

功能逻辑：
- [ ] 服务器选择：官方=内置免费节点下拉；自定义=填房主/playit 地址
- [ ] 虚拟 IP：固定段 10.10.网段.后缀，网段相同、后缀唯一；支持"自动获取 IP"
- [ ] 连接：校验输入 → 启动 edge.exe（构造 n2n 参数）→ 状态流转 → 断开杀进程
- [ ] 联机配置弹窗：房间/社区、密钥、端口、网卡名、自动IP、管理端口、额外参数（如 -M 1200）
- [ ] 系统工具：安装TAP驱动、卸载TAP驱动、配置防火墙（UAC 提权）
- [ ] 配置持久化 config.json（含主题、服务器历史、网段后缀记忆）

### 1.2 服务端管理包 NtoNServer（给房主）
- [ ] supernode.exe 启动/停止（进程模式）
- [ ] 崩溃守护（检测进程+端口，自动重启）
- [ ] Windows 服务安装/卸载（开机自启，sc.exe）
- [ ] 防火墙一键放行端口
- [ ] 实时日志查看
- [ ] playit.gg 中继接入教程（让家里的 supernode 暴露到公网）

### 1.3 文档
- [ ] 房主部署说明（含 playit.gg）
- [ ] 玩家使用说明（装客户端 → 填地址 → 连接）
- [ ] 版本与许可（n2n GPL-3.0、驱动来源）

---

## 2. 技术架构（分层）

```
┌─────────────────────────────────────────────┐
│  UI 层（C# WinForms，NtoNTier.exe）          │
│  主题系统 / 主窗口 / 弹窗 / 页面切换          │
├─────────────────────────────────────────────┤
│  服务层（C#）                                │
│  EdgeRunner：edge 进程启停/参数构造/日志      │
│  NetTools：Ping/TCP/UDP/管理端口查询/防火墙   │
│  Config：config.json 读写                    │
├─────────────────────────────────────────────┤
│  引擎层（外部二进制，随包分发）               │
│  edge.exe        —— n2n 打洞客户端            │
│  tap-windows.exe —— 虚拟网卡驱动安装器        │
│  (服务端) supernode.exe —— n2n 协调/中继      │
└─────────────────────────────────────────────┘
        │ UDP 3301 注册/打洞/中继
        ▼
  公网中继：playit.gg（免费） 或 公共 supernode
```

- 编译：`csc.exe`（系统自带 .NET Framework 4.8），单文件 exe，任何 Win10/11 免安装运行
- 不引入任何第三方 UI 库；全部控件自绘，保证暗/亮一致观感

---

## 3. 核心逻辑链（状态机）

### 3.1 连接流程
```
[未连接]
   │ 点【连接】
   ▼
[校验] 服务器地址非空 + 虚拟IP合法(网段0-255, 后缀1-254)
   │ 失败 → 弹窗提示，留在未连接
   ▼
[启动 edge] 构造参数 → Process.Start(edge.exe)
   │ 退出码/立即退出 → 提示"驱动未装或参数错误"
   ▼
[已连接] 状态绿点 + 按钮变【断开】
   │ 后台每3秒探活：进程存活?
   ├─ 存活 → 保持；刷新在线成员（每5秒）
   └─ 退出 → 自动回到[未连接]，提示掉线
[断开] Kill edge 进程 → 回到[未连接]
```

### 3.2 edge 参数构造（关键，已按 n2n 3.1.1 man 页核实）
```
edge.exe
  -d <网卡名>       默认 n2n_tap
  -c <社区>         所有人相同（默认 tudou）
  -k <密钥>         所有人相同（留空=明文）
  -a <IP>/24        静态：10.10.网段.后缀；自动：dhcp:0.0.0.0
  -l <服务器:端口>  官方/自定义服务器
  -r               开启路由（游戏互通必需）
  -E               接受组播（局域网游戏发现必需）
  -i <秒>           打洞保活间隔（默认20）
  -p <本机端口>     可选，固定利于打洞稳定
  -t <管理端口>     默认5644，供"在线成员"查询
  [额外参数]        -M 1200 等透传
```

### 3.3 打洞与中继（n2n 自动，无需用户干预）
```
玩家A edge ──注册──▶ supernode（协调）
玩家B edge ──注册──▶ supernode
   │ supernode 交换双方公网映射
   ├─ 能打洞（NAT为锥形）→ P2P 直连，流量不过中继（延迟最低）
   └─ 打不通（对称NAT/CGNAT）→ 自动改由 supernode/中继转发（保连通）
```

### 3.4 在线成员发现
```
edge -t 管理端口（UDP 127.0.0.1:5644）
   └─ 发送 "status\n" → 解析返回文本中的虚拟IP
        ├─ 本机IP（10.10.网段.后缀）
        └─ 其它在线成员IP → 列表显示
```

### 3.5 主题切换
```
点主题按钮 → 全局 Theme.Current 切换(暗/亮色板) → 递归重绘全部控件 → 存 config.json
```

---

## 4. 数据模型（config.json）

| 字段 | 类型 | 说明 | 默认 |
|---|---|---|---|
| Dark | bool | 暗色主题 | true |
| Community | string | 房间/社区 | tudou |
| Key | string | 加密密钥 | 空 |
| SupernodePort | int | 服务器端口 | 3301 |
| TapName | string | 虚拟网卡名 | n2n_tap |
| AutoIP | bool | 自动获取IP | false |
| ExtraArgs | string | 额外参数 | 空 |
| RegInterval | int | 打洞保活间隔 | 20 |
| LocalPort | int | 本机固定端口 | 0(随机) |
| MgmtPort | int | 管理端口 | 5644 |
| NetworkPrefix | string | 固定IP段 | 10.10 |
| Segment | string | 网段(记忆) | 30 |
| Suffix | string | 后缀(记忆) | 100 |
| OfficialServers | string | 官方服务器列表(多行) | lucktu 公共节点 |
| CustomServer | string | 最近自定义服务器 | 空 |

---

## 5. UI/UX 设计

### 5.1 布局
```
┌────────┬──────────────────────────────┐
│NtoNTier│ ● 已连接(绿)/未连接           │ ← 顶部状态
│  联机功能│ ┌──────────────────────────┐│
│  在线成员│ │ ① 选择服务器 官方|自定义  ││
│  网络测试│ │    [免费服务器1 ▾]        ││
│  使用指南│ └──────────────────────────┘│
│        │ ┌──────────────────────────┐│
│ [☀亮色]│ │ ② 配置虚拟IP 10.10.网段.后缀││
│        │ └──────────────────────────┘│
│        │   [更多▾]            [连 接] │
└────────┴──────────────────────────────┘
```

### 5.2 配色（两套，按钮/状态/文字全跟随）
- 暗色：底色 #1E2130，卡片 #262A3A，主文字 #E8EAF0，强调绿 #2EA043
- 亮色：底色 #F4F5F7，卡片 #FFFFFF，主文字 #1A1B1C，强调绿 #2EA043
- 语义色：成功绿 / 警告黄 / 错误红，两套主题各自适配

### 5.3 交互细节
- 连接按钮：绿=未连，红=已连（与视频一致）
- 所有按钮扁平圆角、带悬停反馈；输入框带占位提示文字
- 页面级中文提示（如"网段所有人相同，后缀每人不同"）降低理解成本

---

## 6. 边界情况与容错（逐条处理）

| 场景 | 处理 |
|---|---|
| edge.exe 缺失 | 启动前检测，提示放同目录 |
| TAP 驱动未装 | 连接后检测网卡，提示"更多→安装TAP"；提供一键安装 |
| 端口被占用 | 启动失败读 stderr 日志，提示先停止旧的 |
| 服务器不可达 | edge 会持续重试；界面提示"正在连接，若长时间不行检查服务器地址" |
| 网段/后缀非法 | 前端校验拦截（网段0-255、后缀1-254） |
| 多开冲突 | 固定本机端口冲突时提示；默认随机端口规避 |
| 进程崩溃 | 状态轮询自动回"未连接"；不掉 UI 假死 |
| UAC 授权 | 客户端 manifest=requireAdministrator（edge 创建 TAP 需要管理员）；装TAP/防火墙用 runas 提权，取消则提示"未授予权限" |
| 网络测试卡界面 | 全部测试在后台线程执行，结果回调刷新 UI，不阻塞 |
| 双 edge 同机测试 | 需不同网卡名(-d)、本机端口(-p)、管理端口(-t)；联机配置已支持 |
| 杀软误报 | 文档说明 edge.exe 来源（lucktu 编译的 n2n），可加白名单 |
| config 损坏 | 解析失败回默认值，不崩溃 |
| 32/64位 | 编译 AnyCPU，edge 用 x64（本机与主流均为 x64） |

---

## 7. 风险与依赖

| 风险 | 应对 |
|---|---|
| n2n GPL-3.0 许可 | 不闭源混淆；文档标注版权与来源（lucktu/n2n、ntop/n2n） |
| playit.gg 免费版限制（4 UDP 端口） | 每个 supernode 只占 1 个 UDP 端口，够用；教程说明 |
| 中继延迟（打洞失败时） | 界面网络测试可验证直连/中继；文档建议尽量直连 |
| 公共 supernode 稳定性 | 提供"官方服务器"多节点 + 自定义，可替换 |
| edge.exe 被杀软误报 | 文档提供白名单指引 |
| 断线重连 | 边缘节点自动重注册（n2n 特性），无需手动 |

---

## 8. 测试计划（交付前必须做）

1. **本地连通验证**：本机起 supernode(3301) → 用本工具连接 → TAP 网卡创建成功 → 在线成员出现本机
2. **双节点验证**：本机同时起 2 个 edge（不同后缀、不同本机端口）→ 互 Ping 通 → 验证打洞/中继链路
3. **网络测试功能**：Ping/TCP/UDP 三个测试在虚拟局域网内实测
4. **主题切换**：亮/暗各截图检查可读性
5. **容错**：缺 edge.exe / 非法输入 / 端口占用 各跑一遍
6. **打包环境**：把客户端目录拷到干净目录再启动，确认免安装可跑

---

## 9. 交付物清单

```
dist/
├─ client/                 # 发给玩家的客户端
│  ├─ NtoNTier.exe         # 主程序（自绘暗/亮主题）
│  ├─ edge.exe             # n2n 客户端引擎
│  ├─ tap-windows.exe      # 虚拟网卡驱动
│  └─ config.json          # 首次运行自动生成
├─ server/                 # 房主服务端包 NtoNServer
│  ├─ supernode.exe
│  ├─ 启动器/管理面板
│  ├─ playit-接入教程.md
│  └─ 使用说明.html
└─ 使用说明.md              # 小白总览
```

---

## 10. 里程碑

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 规划 | 本文档 | 用户确认无问题 |
| P2 客户端 | GUI + 连接逻辑 + 网络测试 + 主题 | 本地编译运行、截图检查 |
| P3 服务端 | supernode 管理包 + playit 接入 | 本地起服 + edge 连通 |
| P4 联调 | 双节点虚拟局域网打通 | 互 Ping 通、成员可见 |
| P5 交付 | 文档 + 打包 + 使用说明 | 用户能拿到即用 |

---

> 备注：以上为完整规划。任何一栏有异议请直接指出，我修改确认后再动工。
